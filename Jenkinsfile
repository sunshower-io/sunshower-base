pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo "hello, world!"'
                sh 'ls -la'
                sh 'find /'
            }
        }

    }
}



jdbc:
  username: sa
  url: jdbc:h2:mem:
  driver-class: org.h2.Driver

jpa:
  provider:
    name: hibernate
    dialect: org.hibernate.dialect.H2Dialect
    ddl:
      generate: false
      show-sql: true
      format-sql: true
